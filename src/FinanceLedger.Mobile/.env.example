# Copy to .env and fill in. Expo reads EXPO_PUBLIC_* at build time.
# Also mirrored in app.json -> expo.extra.apiBaseUrl as a fallback.
EXPO_PUBLIC_API_BASE_URL=https://api.financeledgerpro.example.com
