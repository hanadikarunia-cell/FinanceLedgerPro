/** Single source of truth for the web app's version — keep in sync with package.json.
 * Shown in the sidebar footer and attached to every feedback submission so triage
 * knows which build a report applies to. */
export const APP_VERSION = '1.2.0';
